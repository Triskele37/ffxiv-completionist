- On Laptop
	- Some components had solid black background on several themes
		- Prime Tab, Inputs, History Overlay, Divider text
		- different npm install?
	- Dev save wasn't default (went to standard Roaming)
- Probably both
	- Bookmarks & Custom need a "no data" display
	- Nav-drawer item needs tooltip on higher element (isn't on full rectangle)
	- Work "disableCompletion" into partials, so standard Emotes don't cause immediate completion for a fresh save

- Responsive (@min-width 750)
	- Prime tab content overflows w/o wrap
		- also see save location string
	- Table headers overlap when table is small (real bad)
	- Large filter select overlay becomes unreadable (use patch view small X)
	- Table overlays don't adjust left
	- ellipsis overflowed summary-lines don't have tooltip
	


