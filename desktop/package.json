{
    "name": "ffxiv-completionist",
    "version": "1.0.6",
    "main": "./main/main.js",
    "scripts": {
        "build": "npm run electron:serve-tsc && ng build --base-href ./",
        "build:dev": "npm run build -- -c dev",
        "build:prod": "npm run build -- -c production",
        "electron": "electron",
        "electron:build": "npm run build:prod && electron-builder build --publish=never",
        "electron:build-all": "npm run build:prod && electron-builder build -mwl --publish=never",
        "electron:build-linux": "npm run build:prod && electron-builder build --linux --publish=never",
        "electron:build-mac": "npm run build:prod && electron-builder build --mac --publish=never",
        "electron:local": "npm run build:prod && electron .",
        "electron:serve": "wait-on tcp:4200 && npm run electron:serve-tsc && electron . --serve",
        "electron:serve-tsc": "tsc -p tsconfig.electron.json",
        "lint": "ng lint",
        "ng": "ng",
        "ng:serve": "ng serve -c web",
        "test": "jest",
        "start": "npm-run-all -p electron:serve ng:serve"
    }
}
