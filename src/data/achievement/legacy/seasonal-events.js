import { DataGroup } from "../../DataGroup";

export const Achievements_Legacy_Seasonal_Events = function(name, parent) {
    return new DataGroup(name, parent).initializeTasks(tasks);
};

const tasks = [
    //TODO: 25 tasks
];
