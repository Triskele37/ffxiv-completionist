import { DataGroup } from "../../../../DataGroup";

export const Duty_Quests_Levequests_Blacksmithing = function(parent) {
    return new DataGroup("Blacksmithing", parent).initializeTasks(tasks);
};
