import { DataGroup } from "../../../../DataGroup";

export const Duty_Quests_Levequests_Leatherworking = function(parent) {
    return new DataGroup("Leatherworking", parent).initializeTasks(tasks);
};
