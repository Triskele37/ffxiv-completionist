name: Build App

on:
    push:
        tags:
            - "v*.*.*"  # Run only on version tags (e.g., v1.0.0)
    workflow_dispatch:  # Allow manual triggers

jobs:
    build:
        runs-on: ${{ matrix.os }}

        strategy:
            fail-fast: false
            matrix:
                os: [windows-latest, macos-latest, ubuntu-latest]
                arch: [x64, arm64]
                exclude:
                    - os: windows-latest
                      arch: arm64 # no build

        steps:
            - name: Checkout
              uses: actions/checkout@v3

            - name: Setup Node
              uses: actions/setup-node@v3
              with:
                  node-version: 18

            - name: Install Dependencies
              run: npm install

            - name: Build (Windows)
              if: matrix.os == 'windows-latest'
              env:
                GH_TOKEN: ${{ secrets.GH_TOKEN }}
              run: npm run electron:build

            - name: Build (macOS)
              if: matrix.os == 'macos-latest'
              env:
                  GH_TOKEN: ${{ secrets.GH_TOKEN }}
              run: npm run electron:build-mac -- --${{ matrix.arch }}

            - name: Build (Linux)
              if: matrix.os == 'ubuntu-latest'
              env:
                  GH_TOKEN: ${{ secrets.GH_TOKEN }}
              run: npm run electron:build-linux -- --${{ matrix.arch }}

            - name: Upload Artifacts
              if: success()
              uses: actions/upload-artifact@v4
              with:
                  name: FFXIV-Completionist-${{ matrix.os }}-${{ matrix.arch }}
                  path: release/
